# Flask-Migrate working directory. Run from backend/:
#   flask --app run:app db init
#   flask --app run:app db migrate -m "initial"
#   flask --app run:app db upgrade
# seed_db.py also calls db.create_all() for local development.
