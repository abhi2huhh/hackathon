export function Footer() {
  return (
    <footer className="border-t border-line px-5 py-10 text-sm text-mute">
      <div className="mx-auto flex max-w-6xl flex-col gap-3 md:flex-row md:items-center md:justify-between">
        <p>InnoLearn — an original interface for learning and technical craft.</p>
        <p>Estimated ATS scores are compatibility signals, not employer ATS results.</p>
      </div>
    </footer>
  );
}
